# Media tools

This directory is populated by `Scripts/bootstrap-tools.sh`. Executable files
are intentionally ignored by Git; release builds copy them into
`Clipped.app/Contents/Helpers/MediaTools`.

Pinned versions, checksums, upstream sources, licenses, and the FFmpeg build
configuration are recorded by the bootstrap script.
